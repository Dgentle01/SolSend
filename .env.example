# This project no longer requires backend database credentials.
# Use `frontend/.env.example` as a reference for frontend environment variables.

# Frontend environment (example)
REACT_APP_BACKEND_URL=https://your-app-name.vercel.app
REACT_APP_SOLANA_NETWORK=mainnet-beta